import React from 'react';
import '../../logo.svg';

const Logo = () => {
    return (
        <svg className="preface-logo">
            <use xlinkHref="#logo" />
        </svg>
    );
};

export default Logo;
